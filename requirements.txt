numpy>=1.26.0
pandas>=2.1.0
openpyxl>=3.1.0  # ranked XLSX submission output
# Pinned <1.6: scikit-learn 1.6 removed the estimator-tags API that the
# XGBoost/LightGBM sklearn wrappers rely on inside VotingClassifier, and the
# bundled model artifacts were trained on 1.5.x. 1.5.2 is the verified version.
scikit-learn>=1.4.2,<1.6
xgboost>=2.0.0
lightgbm>=4.0.0
catboost>=1.2.0
imbalanced-learn>=0.11.0
streamlit>=1.30.0
# Multi-Vector Semantic Layer (Feature 1). Gracefully falls back to a
# scikit-learn TF-IDF encoder if these are unavailable at runtime.
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
# huggingface-hub 1.x drops the API the installed transformers stack expects
# (transformers requires <1.0). Pinning keeps the real transformer encoder
# usable; without it the layer silently degrades to TF-IDF.
huggingface-hub>=0.34.0,<1.0
